# 구즉 미니게임 디자인 시스템 — Jelly Pop

이 문서는 알까기, 틱택토, 양궁의 **규칙과 입력 의미는 바꾸지 않고** 시각 UI, 모션, 효과음, BGM을 하나의 어린이 친화적 경험으로 리메이크하기 위한 기준이다. 모든 신규 스타일은 `[data-gj-theme="candy"]` 아래에만 적용해 홈과 펫 앱의 기존 화면을 침범하지 않는다.

## 0. Research Log

- 저장소의 세 게임 화면, 규칙 모듈, Canvas2D 렌더러, 공용 UI, `apps/pet`의 절차적 Web Audio 구현을 조사했다.
- 기존 기획 세션에서 조사·아트 디렉션·심사·명세를 포함한 40개 작업 중 30개 결과를 복구했다. 네 방향 가운데 **Jelly Pop**을 최종 방향으로 선택했다.
- 선택 이유: 초등학생에게 즉시 읽히는 고채도 색, 큰 터치 표적, 장난감 같은 깊이감, 파일 자산 없이도 CSS/Canvas/Web Audio로 일관되게 구현 가능한 점.
- 핵심 사용성 위험: 틱택토의 “네 번째 수를 놓으면 가장 오래된 말이 사라짐”, 알까기의 `GROW`/`ANCHOR`, 양궁의 바람·시간·명중 판정을 색만으로 전달하면 이해가 어렵다.
- 연령 안전장치: 모든 요소에 얼굴을 붙이지 않는다. 10–12세에게 지나치게 유아적으로 보이지 않도록 기하 형태, 스포츠 배지, 명확한 문구를 사용한다.

## 1. Atmosphere & Identity

- 분위기: 젤리, 스티커, 운동회, 보드게임 장난감을 섞은 밝고 힘 있는 아케이드.
- 형태: 16–24px의 둥근 모서리, 진한 포도색 외곽선, 아래쪽으로 눌린 캔디형 그림자.
- 배경: 크림색 바탕 위에 낮은 대비의 점·별·구름 패턴. 경기판은 가장 높은 대비를 유지한다.
- 상태 표현은 항상 **색 + 형태 + 짧은 문구/아이콘**을 함께 사용한다.
- 각 게임의 개성은 유지한다.
  - 틱택토: 젤리 스티커와 이어지는 승리 리본.
  - 알까기: 나무 보드, 묵직한 돌, 충돌 스파크.
  - 양궁: 맑은 운동장, 바람 리본, 과녁 축제 효과.

## 2. Color Palette

| Token | Value | Purpose |
| --- | --- | --- |
| `--gj-candy-bg` | `#FFF3DB` | 전체 크림 배경 |
| `--gj-candy-surface` | `#FFFFFF` | 카드와 경기판 |
| `--gj-candy-surface-2` | `#FFE0B8` | 보조 패널 |
| `--gj-candy-ink` | `#2B1E3D` | 본문·외곽선 |
| `--gj-candy-ink-soft` | `#5A4A6E` | 보조 문구 |
| `--gj-candy-sky` | `#8FDDF7` | 하늘·정보 |
| `--gj-candy-grass` | `#B6EFA0` | 운동장·긍정 배경 |
| `--gj-candy-p1` | `#FFB020` | 플레이어 X/1 |
| `--gj-candy-p2` | `#2477C7` | 플레이어 O/2 |
| `--gj-candy-accent` | `#FFC93C` | 주요 행동·보상 |
| `--gj-candy-grape` | `#7C3AED` | 특수 상태·스킬 |
| `--gj-candy-spark` | `#FF3D8A` | 축하·강조 |
| `--gj-candy-success` | `#2FBF6B` | 성공 면 |
| `--gj-candy-success-ink` | `#177A45` | 성공 텍스트 |
| `--gj-candy-danger` | `#D81E3C` | 오류·위험 |
| `--gj-candy-danger-ink` | `#A8102A` | 위험 텍스트 |
| `--gj-candy-focus` | `#5B21B6` | 키보드 포커스 |

텍스트는 크림색 배경에서 `--gj-candy-ink`를 기본으로 사용한다. 노랑·연두 면 위에는 흰색 본문을 쓰지 않는다.

## 3. Typography

- 시스템 한글 폰트를 우선해 추가 네트워크 의존성을 만들지 않는다: `Pretendard`, `SUIT`, `Noto Sans KR`, sans-serif.
- 제목: 800–900 weight, 짧고 행동 중심인 문구.
- 본문: 600–700 weight, 최소 15px, 게임 핵심 상태는 최소 17px.
- 숫자(점수·시간·방 코드): tabular numerals, 굵기 800 이상.
- 어린이에게 모호한 축약어 대신 “내 차례”, “다음 수에 사라져요”, “바람이 오른쪽으로 불어요”처럼 완결된 문구를 쓴다.

## 4. Spacing & Layout

- 기본 간격 단위: 4px. 주요 간격: 8, 12, 16, 24, 32px.
- 터치 표적은 최소 44×44px, 핵심 행동은 최소 48px 높이.
- 콘텐츠 최대 폭은 게임에 따라 720–1120px이며 작은 화면에서는 16px 여백을 유지한다.
- 게임 화면 계층: `상단 선수/점수 → 중앙 경기장 → 하단 상태/행동`.
- 안전 영역과 가로/세로 회전을 고려하고, 360px 폭에서도 가로 스크롤이 없어야 한다.

## 5. Component Rules

- `CandyButton`: 진한 외곽선, 아래쪽 눌림 그림자, hover는 살짝 상승, active는 눌림. disabled는 채도뿐 아니라 패턴/투명도와 `cursor`로 구분한다.
- `PlayerChip`: 플레이어 색, 기호, 이름, 현재 차례 배지를 함께 보여준다.
- `GameStatus`: 한 번에 하나의 핵심 행동만 말한다. 변화는 `aria-live="polite"`로 알린다.
- `GameMark`: X/O는 텍스트 글리프가 아니라 CSS/SVG 기하 도형으로 그리며 색 외에도 형태로 구분한다.
- `Countdown`: 숫자, 원형 진행, 경고 문구를 함께 사용한다.
- `Toast`: 중요한 사건의 원인과 결과를 1–2문장으로 설명한다.
- `ResultOverlay`: 승패를 색만으로 구분하지 않고 제목·아이콘·설명을 제공한다. 결과가 확정된 뒤 짧은 연출 후 입력을 받는다.
- `MuteButton`: 음소거 상태를 아이콘과 문구/접근성 이름으로 제공하며 세 게임이 하나의 저장 키를 공유한다.

## 6. Motion Language

- 기본 UI 전환: 140–220ms. 결과/축하: 500–900ms. 물리 결과를 가리는 긴 연출은 금지한다.
- easing: 진입은 `cubic-bezier(.2,.8,.2,1)`, 눌림은 빠른 ease-out, 젤리 튕김은 짧은 overshoot 한 번만 사용한다.
- 틱택토: 놓기 `pop`, 사라질 말 `pulse + 작은 시계`, 교체 발생 시 새 말에서 옛 말로 인과 리본, 승리 줄 `sweep`.
- 알까기: 당김 게이지, 발사 스쿼시, 충돌 스파크, 낙하 링. 물리 좌표/판정은 렌더 연출과 분리한다.
- 양궁: 바람 리본, 시위 장력, 화살 궤적, 명중 링·점수 팝업. 실제 궤적과 판정은 변경하지 않는다.
- `prefers-reduced-motion: reduce`에서는 이동/회전/파티클을 제거하고 80ms 이하의 불투명도 변화만 허용한다.

## 7. Depth & Effects

- 기본 외곽선: 3px `--gj-candy-ink`; 작은 요소는 2px.
- 캔디 그림자: `0 6px 0`의 불투명 포도색 또는 플레이어색 어두운 변형.
- 카드에는 외곽선 + 짧은 그림자 하나만 사용한다. 유리 효과와 과도한 블러는 사용하지 않는다.
- Canvas 효과는 최대 입자 수와 수명을 제한하고, 장식은 게임 상태를 수정하지 않는 별도 렌더 계층에서만 계산한다.
- 사운드는 Web Audio 절차 합성으로 구현한다. BGM/효과음 버스를 분리하고 동시 효과음을 제한하며, 첫 사용자 제스처 전에는 재생하지 않는다.

## 8. Accessibility & Accepted Debt

- 키보드 포커스는 3px 포커스 링과 3px 간격으로 항상 보인다.
- 모든 상호작용은 색 외의 단서를 제공하며, 상태 변화는 화면 문구와 live region으로 전달한다.
- 결과 오버레이가 열리면 주요 버튼으로 포커스를 이동하고, 닫힌 뒤 적절한 위치로 되돌린다.
- 음소거와 모션 감소를 존중한다. 갑작스러운 고주파·과도한 음량·연속 진동 효과는 금지한다.
- 허용 부채: 알까기의 정밀 키보드 조준 대체 입력은 별도 접근성 설계가 필요하다. 이번 리메이크에서는 명확한 드래그 안내와 큰 표적을 우선하고, 키보드 전용 플레이는 후속 범위로 기록한다.
- 검증 기준: 360/768/1280px, 키보드만 사용, reduced motion, 음소거 유지, 핵심 상태의 색맹 비의존성, 10–12세에게 유아적으로 보이지 않는지 점검한다.

## 9. Festival Feedback Expansion (Tic-tac-toe & Alkkagi)

- 적용 범위는 틱택톡과 알까기다. 양궁의 `sport` BGM, 화면, 효과음은 이번 확장에서 변경하지 않는다.
- BGM은 사용자 입력 뒤 시작하는 절차 합성 음악으로 유지하되, `strategy`는 장난감 오르골의 리드·베이스·반짝음, `arcade`는 빠른 리드·베이스·킥의 3층 구조를 사용한다. 효과음은 BGM보다 앞에 들리도록 짧고 강하게 끝난다.
- `ImpactBurst`: 말을 놓거나 돌이 충돌한 위치에서 중심 링 → 8방향 파편 → 짧은 잔광 순서로 700ms 안에 끝난다.
- `StateSplash`: 턴 변경, 링 아웃, 스킬 준비처럼 의미가 바뀌는 순간에만 글자가 blur 6px + 아래 40% 위치에서 들어와 overshoot 후 사라진다. beui.dev `text-animation`과 `animated-badge`의 상태 교체 원리를 CSS로 옮긴다.
- `ArenaPulse`: 현재 턴 색으로 경기장 뒤의 방사광과 가장자리 광택이 바뀐다. 화면 전체 흔들림은 충돌·탈락 같은 물리 사건에만 420ms 이하로 사용한다.
- 승리는 3단계로 읽혀야 한다: 이긴 오브젝트의 순차 팝 → 보드/경기장 광휘 → 색종이와 결과 오버레이. 규칙 판정이나 입력 가능 시점은 연출과 분리한다.
- 새 모션 토큰: `--gj-burst: 680ms`, `--gj-impact: 420ms`, `--gj-celebrate: 1100ms`, 진입 곡선 `cubic-bezier(.16,1.35,.4,1)`, 퇴장 곡선 `cubic-bezier(.4,0,1,1)`.
- `prefers-reduced-motion: reduce`에서는 이동·회전·연속 배경 애니메이션을 제거하고, 상태 변화는 80ms 이하의 불투명도 전환과 정적인 링/강조색으로 남긴다. 효과 레이어는 항상 `pointer-events: none`이고 접근성 트리에서 제외한다.
